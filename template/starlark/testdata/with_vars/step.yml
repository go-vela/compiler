steps:
  - name: sample
    template:  
      name: build
      vars:
        tags: [latest, "1.14", "1.15"]
        pull_policy: always
        commands:
          build: "go test ./..."
          test: "go build"